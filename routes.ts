/**
 * Public routes
 * @type {string[]}
 */
export const publicRoutes = ["/", "/home", "/auth/sign-in", "/auth/sign-up"];

export const authRoutes = ["/auth/sign-in", "/auth/sign-up"];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGGED_IN_REDIRECT = "/dashboard";
